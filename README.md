# hhh-cot-monitoring

Evaluate **Chain-of-Thought (CoT) monitorability** for a base model and an RL-trained model on the same dataset.


## Install
```bash
python -m venv .venv && source .venv/bin/activate         
pip install --upgrade pip
pip install -r requirements.txt
pip install -e .
